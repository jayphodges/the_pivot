<table class="table">
  <thead>
    <tr>
      <th>Image</th>
      <th>Title</th>
      <th>Description</th>
      <th>Price</th>
      <th>Status</th>
      <th>Edit</th>
    </tr>
  </thead>
  <tbody>
    <% @items.each do |item| %>
    <tr class="item">
      <th><img class="admin-item-thumb" src="<%= item.image.url %>" alt=""></th>
      <td><%= link_to item.title, item_path(item) %></td>
      <td><%= item.description %></td>
      <td>$<%= item.formatted_price %></td>
      <td><%= item.status %></td>
      <td class="Edit"><%= link_to "Edit", edit_admin_item_path(item) %></td>
    </tr>
    <% end %>
  </tbody>
</table>
